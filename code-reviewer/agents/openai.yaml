interface:
  display_name: "Code Reviewer"
  short_description: "Code CR with cascade analysis"
  default_prompt: "Use $code-reviewer to review this code change and return findings, risk, and recommendation without posting comments."
