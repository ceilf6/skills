interface:
  display_name: "Issue Reviewer"
  short_description: "Issue quality assessment and triage"
  default_prompt: "Use $issue-reviewer to analyze this issue and return quality score, priority suggestion, and improvement suggestions without posting comments."
